# Historical Notes (v3)

This document previously contained benchmark results for an older configuration.  
To avoid conflicting numbers, the authoritative, up‑to‑date benchmark description is now in `benchmarks.md`.

Please refer to `benchmarks.md` for:

- Current benchmark methodology (as implemented in `bench/bench.cpp`).
- PUT / GET / QUERY results for the 10,000‑document workload.

**TODO:** If needed, re‑introduce versioned benchmark history here with clear labels (e.g. by engine version and configuration).